<%= form_with(model: room, local: true) do |form| %>
  <% if room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :ident %>
    <%= form.text_field :ident, id: :room_ident %>
  </div>

  <div class="field">
    <%= form.label :owner %>
    <%= form.number_field :owner, id: :room_owner %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
